{% extends "page.twig" %}

{% block pageContent %}
    <div id="aboutMeImgContainer">
        <img id="aboutMeImg" src="{{ post.thumbnail.src }}" alt="{{ post.thumbnail.alt }}"/>
    </div>
    {{post.content}}
{% endblock %}